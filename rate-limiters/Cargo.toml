[package]
name = "rate-limiters"
version = "0.1.0"
edition = "2024"

[dev-dependencies]
criterion = {version = "0.8.2", features=["html_reports"]}

[[bench]]
name = "rate_limiter_bench"
harness = false
